# Jenkins CI Demo

This is a simple CI demo project using Jenkins.

## What the app does
- Prints app version and build timestamp
- Shows hostname and current date
- Displays system information:
  - OS
  - Kernel version
  - CPU info
  - Memory

## How to run

```bash
chmod +x app.sh test.sh
./app.sh
./test.sh
# trigger build
